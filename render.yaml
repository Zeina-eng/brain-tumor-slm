services:
  - type: web
    name: brain-tumor-slm-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: HF_API_KEY
        sync: false
